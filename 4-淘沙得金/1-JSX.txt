
JSX是什么

	1、JSX全称是 JavaScript XML，在js中写XML标签，是JavaScript的一种扩展语法

	2、JSX标签既不是字符串，也不是HTML，编译后是一个对象，描述要显示UI信息
		说明：XML是可扩展标记语言。
		可扩展，是指除了可以写规定好的标签外，还可以写自己定义的标签

	3、可以写入的标签：
		（1）w3c规定的标签，浏览器端能够解析
		（2）自定义标签，首字母要大写，是封装的组件
		（3）任意标签，不被浏览器解析